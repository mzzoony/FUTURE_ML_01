# Trained Models

Trained model files will be saved here.

**Note:** Large model files should not be committed to Git. Consider using:
- Git LFS for large files
- Cloud storage (Google Drive, AWS S3)
- Model registries (MLflow, Weights & Biases)

Add model files to `.gitignore` if they are large.
